// Test stub for `@docusaurus/useBaseUrl`.
//
// Returns the path unchanged, which is sufficient for component tests that
// only need a resolvable, deterministic value.

export default function useBaseUrl(path: string): string {
  return path;
}
